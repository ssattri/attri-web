import type{Metadata}from"next";import ProductDetailClient from"./ProductDetailClient";export const dynamic="force-dynamic";export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const title=slug.split("-").map(x=>x.charAt(0).toUpperCase()+x.slice(1)).join(" ");return{title:`${title} | Attri Associates`,description:`Explore ${title} from Attri Associates. View product details, availability, delivery information and specifications.`,keywords:[title,"Attri Associates","Vastu products","architecture services"],alternates:{canonical:`/shop/${slug}`},openGraph:{title:`${title} | Attri Associates`,description:`Explore ${title} from Attri Associates.`,type:"website",url:`/shop/${slug}`}}}export default async function ProductDetailPage({params}:{params:Promise<{slug:string}>}){const{slug}=await params;return <ProductDetailClient slug={slug}/>}
