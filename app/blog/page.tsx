import SiteHeader from"../components/SiteHeader";import SiteFooter from"../components/SiteFooter";export const dynamic="force-dynamic";
type Post={id:number;title:string;slug:string;excerpt:string;category:string;author:string;coverImageUrl:string;publishedAt:string;readingMinutes:number;featured:number};
async function posts(){try{return(await(await import("cloudflare:workers")).env.DB.prepare("SELECT id,title,slug,excerpt,category,author,cover_image_url AS coverImageUrl,published_at AS publishedAt,reading_minutes AS readingMinutes,featured FROM blog_posts WHERE status='published' AND (published_at='' OR published_at<=CURRENT_TIMESTAMP) ORDER BY featured DESC,CASE WHEN published_at='' THEN created_at ELSE published_at END DESC").all<Post>()).results}catch{return[]}}
export default async function Blog(){const list=await posts(),lead=list[0];return <main className="knowledge-page"><SiteHeader active="/blog"/><section className="knowledge-hero"><div><p>ATTRI KNOWLEDGE CENTER</p><h1>Ideas for better<br/><em>spaces and lives.</em></h1><span>Research, field experience and practical guidance across Architecture, Engineering, Vastu and Construction.</span></div></section>{lead?<a className="featured-article" href={`/blog/${lead.slug}`}><div>{lead.coverImageUrl?<img src={lead.coverImageUrl} alt={lead.title}/>:null}</div><article><small>FEATURED · {lead.category}</small><h2>{lead.title}</h2><p>{lead.excerpt}</p><span>{lead.author} · {lead.readingMinutes} min read</span><b>Read insight →</b></article></a>:null}<section className="article-library"><header><div><small>KNOWLEDGE LIBRARY</small><h2>Latest insights</h2></div></header><div>{list.slice(lead?1:0).map(post=><a href={`/blog/${post.slug}`} key={post.id}><div>{post.coverImageUrl?<img src={post.coverImageUrl} alt={post.title}/>:<span>{post.title.charAt(0)}</span>}</div><small>{post.category}</small><h3>{post.title}</h3><p>{post.excerpt}</p><footer><span>{post.readingMinutes} min read</span><b>Explore →</b></footer></a>)}{!list.length?<p className="knowledge-empty">Published insights will appear here.</p>:null}</div></section><SiteFooter/></main>}
