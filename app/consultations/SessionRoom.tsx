"use client";import{FormEvent,useEffect,useState}from"react";type Data={session:any;messages:any[];viewer:{email:string;role:string}};export default function SessionRoom({reference}:{reference:string}){const[data,setData]=useState<Data|null>(null),[msg,setMsg]=useState("");async function load(){const r=await fetch(`/api/consultations/session?reference=${encodeURIComponent(reference)}`),d=await r.json();if(r.ok)setData(d)}useEffect(()=>{void load();const timer=setInterval(()=>void load(),5000);return()=>clearInterval(timer)},[reference]);async function send(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=e.currentTarget,message=String(new FormData(f).get("message")||"");const r=await fetch("/api/consultations/session",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({reference,message})}),d=await r.json();if(!r.ok)setMsg(d.error);else{f.reset();await load()}}async function status(value:string){await fetch("/api/consultations/session",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({reference,status:value})});await load()}if(!data)return <div className="session-loading">Opening secure consultation room…</div>;const s=data.session,isConsultant=["consultant","admin"].includes(data.viewer.role);return <div className="session-room"><header><div><small>{s.reference} · {s.consultationMode}</small><h1>{isConsultant?s.userName:s.consultantName}</h1><p>{s.scheduledDate} · {s.scheduledTime} · {s.durationMinutes} minutes</p></div><span className={`session-status ${s.status}`}>{s.status}</span></header><section className="session-mode-panel">{s.consultationMode==="video"?<div><span>VIDEO CONSULTATION</span><h2>Your private meeting room</h2>{s.meetingUrl?<a href={s.meetingUrl} target="_blank" rel="noreferrer">Join video meeting ↗</a>:<p>The meeting link will appear after confirmation.</p>}</div>:s.consultationMode==="call"?<div><span>PHONE CONSULTATION</span><h2>Connect by secure call</h2>{s.phone?<a href={`tel:${s.phone.replace(/[^+\d]/g,"")}`}>Call consultant now</a>:<p>Phone details will appear after confirmation.</p>}</div>:<div><span>SECURE CHAT CONSULTATION</span><h2>Message your consultant below</h2><p>Messages stay connected to this consultation record.</p></div>}{isConsultant?<div className="session-controls"><button onClick={()=>status("confirmed")}>Confirm</button><button onClick={()=>status("live")}>Start session</button><button onClick={()=>status("completed")}>Complete</button></div>:null}</section><section className="session-chat"><header><h2>Consultation chat</h2><span>Refreshes automatically</span></header><div>{data.messages.length?data.messages.map(m=><article className={m.senderEmail.toLowerCase()===data.viewer.email.toLowerCase()?"mine":""} key={m.id}><b>{m.senderRole}</b><p>{m.message}</p><time>{new Date(m.createdAt).toLocaleString("en-IN")}</time></article>):<p className="session-empty">No messages yet. Start the conversation.</p>}</div><form onSubmit={send}><input name="message" maxLength={2000} placeholder="Write a message…" required/><button>Send</button></form>{msg&&<small>{msg}</small>}</section></div>}
