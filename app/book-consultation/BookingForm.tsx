"use client";
import {FormEvent,useEffect,useState} from "react";
const times=["10:00 AM – 11:00 AM","11:30 AM – 12:30 PM","2:00 PM – 3:00 PM","3:30 PM – 4:30 PM","5:00 PM – 6:00 PM"],consultants=["Any available consultant","Senior Vastu Consultant","Architecture Consultant","Interior & Design Consultant"];
export default function BookingForm(){const[result,setResult]=useState<{reference?:string;error?:string}|null>(null),[busy,setBusy]=useState(false),[date,setDate]=useState(""),[consultant,setConsultant]=useState(consultants[0]),[available,setAvailable]=useState(times);useEffect(()=>{if(!date){setAvailable(times);return}fetch(`/api/appointments/availability?date=${encodeURIComponent(date)}&consultant=${encodeURIComponent(consultant)}`).then(r=>r.json()).then(d=>setAvailable((d.slots||[]).filter((x:{available:boolean})=>x.available).map((x:{time:string})=>x.time))).catch(()=>setAvailable(times))},[date,consultant]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setResult(null);const form=e.currentTarget,r=await fetch("/api/appointments",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))}),d=await r.json();setBusy(false);if(r.ok){setResult({reference:d.reference});form.reset();setDate("");setConsultant(consultants[0])}else setResult({error:d.error})}if(result?.reference)return <div className="booking-success"><span>✓</span><p>REQUEST RECEIVED</p><h2>Your consultation request is registered.</h2><strong>{result.reference}</strong><p>Our team will contact you to confirm the final time and consultation details.</p><button onClick={()=>setResult(null)}>Book another consultation</button></div>;return <form className="booking-form" onSubmit={submit}><div className="form-section"><span>01</span><div><h3>Choose your consultation</h3><p>Select the service and how you would like to meet.</p></div></div><div className="booking-grid"><label>Service *<select name="service" required><option value="">Select service</option><option>Residential Vastu</option><option>Commercial Vastu</option><option>Industrial / Factory Vastu</option><option>Architecture Planning</option><option>Structural Design</option><option>Interior Design</option><option>Project Review</option></select></label><label>Consultation mode *<select name="consultationMode" required><option value="">Select mode</option><option>Video Consultation</option><option>Phone Consultation</option><option>Chat Consultation</option><option>Office Meeting</option><option>Site Visit</option></select></label><label>Consultant preference<select name="consultantPreference" value={consultant} onChange={e=>setConsultant(e.target.value)}>{consultants.map(x=><option key={x}>{x}</option>)}</select></label><label>Project type<select name="projectType"><option>New Construction</option><option>Existing Property</option><option>Renovation</option><option>Drawing Review</option></select></label><label>Booking type<select name="bookingType"><option value="scheduled">Schedule for later</option><option value="now">Request now</option></select></label></div><div className="form-section"><span>02</span><div><h3>Preferred schedule</h3><p>Only available slots are shown for the selected date and consultant.</p></div></div><div className="booking-grid two"><label>Preferred date *<input type="date" name="preferredDate" required min={new Date().toISOString().slice(0,10)} value={date} onChange={e=>setDate(e.target.value)}/></label><label>Preferred time *<select name="preferredTime" required><option value="">{date&&!available.length?"No slots available":"Select time"}</option>{available.map(slot=><option key={slot}>{slot}</option>)}</select></label></div><div className="form-section"><span>03</span><div><h3>Your information</h3><p>We will use these details only to coordinate your consultation.</p></div></div><div className="booking-grid"><label>Full name *<input name="name" required/></label><label>Phone number *<input name="phone" required inputMode="tel"/></label><label>Email address *<input name="email" required type="email"/></label></div><label className="message-field">Tell us about your requirement<textarea name="message" rows={5} placeholder="Property type, location, approximate area and the guidance you need"/></label>{result?.error&&<p className="booking-error">{result.error}</p>}<button className="booking-submit" disabled={busy||!available.length}>{busy?"Submitting…":"Request consultation"} <span>↗</span></button><small className="privacy-copy">By submitting, you agree to be contacted regarding this consultation request. No payment is collected at this stage.</small></form>}
