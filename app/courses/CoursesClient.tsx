"use client";
import { useEffect, useState } from "react";
type Course={id:number;slug:string;title:string;category:string;level:string;mode:string;duration:string;description:string;price:number;lessons:number;imageUrl:string;instructor:string;certificate:number};
export default function CoursesClient(){const[courses,setCourses]=useState<Course[]>([]),[loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/courses").then(r=>r.json()).then(d=>{setCourses(d.courses??[]);setLoading(false)})},[]);const money=(v:number)=>v?new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(v/100):"Free";if(loading)return <p className="courses-loading">Loading programmes…</p>;return <div className="course-grid">{courses.map((c,i)=><article key={c.id}><div className={`course-cover course-${i%4}`}>{c.imageUrl&&<img src={c.imageUrl} alt={c.title}/>}<span>{c.category}</span>{!c.imageUrl&&<b>{String(i+1).padStart(2,"0")}</b>}<small>{c.mode}</small></div><div><div className="course-meta"><span>{c.level}</span><span>{c.duration}</span><span>{c.lessons} lessons</span>{c.certificate?<span>Certificate</span>:null}</div><h3>{c.title}</h3><p>{c.description}</p><small className="course-instructor">By {c.instructor}</small><footer><strong>{money(c.price)}</strong><a href={`/courses/${c.slug}`}>View course →</a></footer></div></article>)}</div>}
