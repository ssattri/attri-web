"use client";
import { useState } from "react";
export default function EnrollButton({courseId,price}:{courseId:number;price:number}){const[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function enroll(){setBusy(true);const r=await fetch("/api/courses",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({courseId})}),d=await r.json();if(r.status===401){window.location.href="/client/login";return}if(r.ok){setMessage(price?`Enrollment ${d.reference} created. Payment confirmation will activate your course.`:`You are enrolled. Opening your learning dashboard…`);if(!price)setTimeout(()=>window.location.href="/learning",900)}else setMessage(d.error||"Unable to enroll.");setBusy(false)}return <div className="course-enroll-action"><button onClick={enroll} disabled={busy}>{busy?"Enrolling…":price?"Enroll now →":"Start free course →"}</button>{message&&<p>{message}</p>}<small>A registered User account is required. Consultant accounts cannot enroll.</small></div>}
