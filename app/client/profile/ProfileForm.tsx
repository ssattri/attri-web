"use client";import{FormEvent,useEffect,useState}from"react";
export default function ProfileForm(){const[data,setData]=useState<Record<string,string>|null>(null),[message,setMessage]=useState("");useEffect(()=>{fetch("/api/client/profile",{cache:"no-store"}).then(r=>r.json()).then(d=>{if(d.profile)setData(d.profile);else setMessage(d.error||"Unable to load profile.")})},[]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const r=await fetch("/api/client/profile",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))}),d=await r.json();setMessage(r.ok?"Profile saved successfully.":d.error||"Unable to save profile.")}if(!data)return <div className="portal-loading">Loading profile…</div>;return <form className="client-profile-form" onSubmit={save}><div className="profile-form-heading"><p>ACCOUNT DETAILS</p><h2>Your profile</h2><span>Keep your contact and billing details ready for consultations and orders.</span></div><div className="profile-form-grid"><label>Full name<input name="fullName" defaultValue={data.fullName} required/></label><label>Email<input value={data.email} readOnly/></label><label>Mobile number<input name="phone" defaultValue={data.phone} placeholder="+91 99900 00000"/></label><label>Company / organisation<input name="company" defaultValue={data.company}/></label><label className="wide">Address<textarea name="address" defaultValue={data.address} rows={3}/></label><label>City<input name="city" defaultValue={data.city}/></label><label>State<input name="state" defaultValue={data.state}/></label><label>PIN code<input name="pincode" inputMode="numeric" defaultValue={data.pincode}/></label><label>GSTIN (optional)<input name="gstin" defaultValue={data.gstin}/></label></div>{message&&<div className="profile-form-message">{message}</div>}<button>Save profile</button></form>}
