import{getChatGPTUser}from"../../chatgpt-auth";
export const dynamic="force-dynamic";
export default async function ClientLogin(){const user=await getChatGPTUser();let profile:{accountType:string;registrationCompleted:number;status:string}|null=null;if(user){const d=(await import("cloudflare:workers")).env.DB;try{profile=await d.prepare("SELECT account_type AS accountType,registration_completed AS registrationCompleted,status FROM customer_profiles WHERE lower(email)=?").bind(user.email.toLowerCase()).first<{accountType:string;registrationCompleted:number;status:string}>()}catch{profile=null}}const registered=profile?.registrationCompleted===1&&profile.status==="active";const destination=profile?.accountType==="consultant"?"/consultant":"/client";return <main className="client-access-page"><section className="client-access-brand"><a className="brand" href="/"><span className="brand-mark">A</span><span><strong>ATTRI</strong><small>ASSOCIATES</small></span></a><div><p>ONE SECURE ACCOUNT</p><h1>Access your<br/><em>workspace.</em></h1><span>Every User and Consultant dashboard requires verified sign-in followed by one-time account registration.</span></div><div className="access-trust"><b>Verified sign-in</b><b>Required registration</b><b>Role-based access</b></div></section><section className="client-access-card role-access-card">{!user?<><div><p>STEP 1 / SIGN IN</p><h2>Sign in to continue</h2><span>Verify your identity securely. After sign-in, new members must complete registration before any dashboard opens.</span></div><a className="secure-signin-button" href="/signin-with-chatgpt?return_to=%2Fclient%2Flogin"><b>Continue with ChatGPT</b><span>Google, Apple or email authentication →</span></a><small className="access-note">Your password is never shared with or stored by Attri Associates.</small></>:registered?<><div><p>SECURE SESSION</p><h2>Welcome back, {user.fullName||user.displayName}</h2><span>Signed in as {user.email}. Your registered {profile?.accountType} workspace is ready.</span></div><a className="secure-signin-button" href={destination}><b>Open {profile?.accountType} dashboard</b><span>Continue securely →</span></a><a className="access-back" href="/signout-with-chatgpt?return_to=/client/login">Sign out and use another account</a></>:<><div><p>STEP 2 / REGISTRATION</p><h2>Choose your account type</h2><span>Signed in as {user.email}. Complete the required registration form before entering a dashboard.</span></div><a className="role-choice user-choice" href="/client/onboarding?role=user"><strong>01</strong><span><b>Register as User</b><small>Buy courses, products, consultations, services and software subscriptions.</small><em>Complete User registration →</em></span></a><a className="role-choice consultant-choice" href="/client/onboarding?role=consultant"><strong>02</strong><span><b>Register as Consultant</b><small>Use the cloud Vastu Compass, client projects, analyses and reports.</small><em>Complete Consultant registration →</em></span></a><small className="access-note">One verified email can be registered with only one account type.</small></>}<a className="access-back" href="/">← Return to website</a></section></main>}
