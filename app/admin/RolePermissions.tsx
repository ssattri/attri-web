"use client";import{useEffect,useState}from"react";type Staff={id:number;fullName:string;role:string;department:string;permissionsJson:string;status:string};const modules=[["dashboard","Dashboard"],["crm","CRM & Leads"],["projects","Projects"],["appointments","Appointments"],["commerce","Shop & Orders"],["courses","Courses & LMS"],["finance","Finance"],["reports","Reports"],["content","CMS Content"],["files","File Vault"],["analytics","Analytics"],["settings","Settings"]];
export default function RolePermissions(){const[staff,setStaff]=useState<Staff[]>([]);async function load(){const r=await fetch("/api/admin/permissions");if(r.ok)setStaff((await r.json()).staff)}useEffect(()=>{void load()},[]);async function toggle(s:Staff,key:string){const current:string[]=s.permissionsJson?JSON.parse(s.permissionsJson):[];const permissions=current.includes(key)?current.filter(x=>x!==key):[...current,key];const r=await fetch("/api/admin/permissions",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id:s.id,permissions})});if(r.ok)await load()}return <section className="admin-panel role-centre" id="permissions"><div className="panel-title"><div><p>ACCESS CONTROL</p><h2>Staff roles & permissions</h2></div><span>{staff.length} team members</span></div><div className="permission-grid">{staff.length===0?<p className="empty-row">Add staff members in Data Managers to assign access.</p>:staff.map(s=>{const active:string[]=s.permissionsJson?JSON.parse(s.permissionsJson):[];return <article key={s.id}><header><div><b>{s.fullName}</b><small>{s.role} · {s.department}</small></div><span>{s.status}</span></header><div>{modules.map(m=><label key={m[0]}><input type="checkbox" checked={active.includes(m[0])} onChange={()=>toggle(s,m[0])}/><span>{m[1]}</span></label>)}</div></article>})}</div></section>}
