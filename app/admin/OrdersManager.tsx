"use client";
import{useEffect,useState}from"react";
type Order={id:number;reference:string;customerName:string;email:string;phone:string;address:string;city:string;state:string;pincode:string;itemsJson:string;subtotal:number;shippingAmount:number;total:number;paymentMethod:string;status:string;paymentStatus:string;trackingNumber:string;adminNotes:string;createdAt:string};
export default function OrdersManager({initialOrders}:{initialOrders:Order[]}){const[orders,setOrders]=useState(initialOrders),[open,setOpen]=useState<number|null>(null),[message,setMessage]=useState("");useEffect(()=>setOrders(initialOrders),[initialOrders]);const money=(v:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format((v||0)/100);
 async function save(order:Order){const r=await fetch("/api/admin/commerce",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({id:order.id,status:order.status,paymentStatus:order.paymentStatus,trackingNumber:order.trackingNumber,adminNotes:order.adminNotes})});const d=await r.json();setMessage(r.ok?`${order.reference} updated.`:d.error||"Unable to update order.")}
 function change(id:number,key:keyof Order,value:string){setOrders(current=>current.map(o=>o.id===id?{...o,[key]:value}:o))}
 return <div className="order-manager">{message&&<button className="order-manager-message" onClick={()=>setMessage("")}>{message} ×</button>}{orders.length===0?<p className="empty-row">No orders yet.</p>:orders.map(order=>{let items:Array<{name:string;price:number;quantity:number}>=[];try{items=JSON.parse(order.itemsJson)}catch{}return <article className={open===order.id?"order-card expanded":"order-card"} key={order.id}><button className="order-summary" onClick={()=>setOpen(open===order.id?null:order.id)}><span><b>{order.reference}</b><small>{new Date(order.createdAt).toLocaleDateString("en-IN")} · {items.length} line(s)</small></span><span><b>{order.customerName}</b><small>{order.phone}</small></span><span><b>{money(order.total||order.subtotal)}</b><small>{order.paymentMethod.replaceAll("-"," ")}</small></span><i className={`order-status ${order.status}`}>{order.status}</i><em>{open===order.id?"−":"+"}</em></button>{open===order.id&&<div className="order-detail"><section><h3>Items</h3>{items.map((item,i)=><div className="order-item" key={i}><span>{item.name}<small>{money(item.price)} × {item.quantity}</small></span><b>{money(item.price*item.quantity)}</b></div>)}<dl><div><dt>Subtotal</dt><dd>{money(order.subtotal)}</dd></div><div><dt>Shipping</dt><dd>{order.shippingAmount?money(order.shippingAmount):"Free"}</dd></div><div><dt>Total</dt><dd>{money(order.total||order.subtotal)}</dd></div></dl></section><section><h3>Customer & delivery</h3><p><b>{order.customerName}</b><br/>{order.email}<br/>{order.phone}</p><p>{order.address}<br/>{order.city}, {order.state} – {order.pincode}</p></section><form onSubmit={e=>{e.preventDefault();void save(order)}}><h3>Fulfilment</h3><label>Order status<select value={order.status} onChange={e=>change(order.id,"status",e.target.value)}><option>pending</option><option>confirmed</option><option>processing</option><option>shipped</option><option>completed</option><option>cancelled</option></select></label><label>Payment status<select value={order.paymentStatus} onChange={e=>change(order.id,"paymentStatus",e.target.value)}><option>pending</option><option>paid</option><option>failed</option><option>refunded</option></select></label><label>Tracking number<input value={order.trackingNumber||""} onChange={e=>change(order.id,"trackingNumber",e.target.value)}/></label><label>Internal notes<textarea rows={3} value={order.adminNotes||""} onChange={e=>change(order.id,"adminNotes",e.target.value)}/></label><button>Save order update</button></form></div>}</article>})}</div>}
