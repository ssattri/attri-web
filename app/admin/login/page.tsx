import{adminEmail,getAdminUser,safeAdminPath}from"../../admin-auth";import{redirect}from"next/navigation";import Link from"next/link";import AdminLoginForm from"./AdminLoginForm";
export const dynamic="force-dynamic";
export default async function AdminLogin({searchParams}:{searchParams:Promise<{error?:string;return_to?:string;returnTo?:string}>}){const query=await searchParams,returnTo=safeAdminPath(query.return_to||query.returnTo);if(await getAdminUser())redirect(returnTo);return <main className="admin-auth-page"><section aria-labelledby="admin-login-title"><Link className="brand" href="/"><span className="brand-mark">A</span><span><strong>ATTRI</strong><small>ASSOCIATES</small></span></Link><div><p>SECURE ADMINISTRATION</p><h1 id="admin-login-title">Control centre<br/><em>access.</em></h1><span>Administration is isolated from customer accounts and protected by role validation, lockout controls and short owner sessions.</span></div><div className="admin-auth-badges" aria-label="Security features"><b>Admin only</b><b>Attempt lockout</b><b>Audit ready</b></div></section><article aria-label="Administrator sign in"><p>ADMIN LOGIN</p><h2>Authorised personnel only</h2><span>Enter your administrator credentials to continue.</span><AdminLoginForm returnTo={returnTo} defaultEmail={adminEmail()} hasError={Boolean(query.error)}/></article></main>}
