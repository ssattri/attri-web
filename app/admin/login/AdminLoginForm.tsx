"use client";
import {FormEvent,useState} from "react";
export default function AdminLoginForm({returnTo,defaultEmail,hasError}:{returnTo:string;defaultEmail:string;hasError:boolean}){const[error,setError]=useState(hasError?"We could not verify those credentials. Check your email and password, then try again.":"");const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const body=new FormData(e.currentTarget);const r=await fetch("/api/admin/session",{method:"POST",headers:{accept:"application/json","x-admin-login":"1"},body});const d=await r.json().catch(()=>({}));if(r.ok){window.location.assign(returnTo)}else{setError(d.error||"We could not verify those credentials. Check your email and password, then try again.");setBusy(false)}}return <form className="admin-login-form" onSubmit={submit} aria-describedby="login-help"><span className="sr-only" id="login-help">Administrator credentials are required.</span><input type="hidden" name="returnTo" value={returnTo}/>{error&&<div className="admin-login-error" role="alert">{error}</div>}<label>Administrator email<input name="email" type="email" autoComplete="username" inputMode="email" defaultValue={defaultEmail} placeholder="admin@example.com" required autoFocus/></label><label>Password<input name="password" type="password" autoComplete="current-password" placeholder="Your secure password" required/></label><button type="submit" disabled={busy}>{busy?"Verifying…":"Enter control centre →"}</button><a href="/admin/reset-password">Forgot administrator password?</a><a href="/">← Return to website</a></form>}
