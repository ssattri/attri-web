"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";export default function CreatePageForm(){const[message,setMessage]=useState(""),[busy,setBusy]=useState(false),router=useRouter();async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const r=await fetch("/api/cms/pages",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))}),d=await r.json();if(r.ok)router.push("/ss_attri/admin/pages");else{setMessage(d.error||"Unable to create page.");setBusy(false)}}return <form className="cms-create-form" onSubmit={save}><p>CONTENT / SEO</p><h1>Create a page</h1><label>Page title<input name="title" required placeholder="About our studio"/></label><label>URL slug<input name="slug" required placeholder="about-our-studio"/></label><label>Excerpt<textarea name="excerpt" rows={3}/></label><label>Content<textarea name="content" rows={8} placeholder="Page content or HTML"/></label><fieldset><legend>SEO metadata</legend><label>Meta title<input name="seoTitle" maxLength={70}/></label><label>Meta keywords<input name="seoKeywords" placeholder="architecture, Vastu, consultancy"/></label><label>Meta description<textarea name="seoDescription" maxLength={170} rows={3}/></label></fieldset><button disabled={busy}>{busy?"Creating…":"Create draft page"}</button>{message&&<small>{message}</small>}</form>}
