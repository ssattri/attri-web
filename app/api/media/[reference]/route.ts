export async function GET(_request:Request,{params}:{params:Promise<{reference:string}>}){const{reference}=await params,e=(await import("cloudflare:workers")).env,row=await e.DB.prepare("SELECT object_key AS objectKey,content_type AS contentType,file_name AS fileName FROM media_assets WHERE reference=? AND status='active'").bind(reference).first<{objectKey:string;contentType:string;fileName:string}>();if(!row)return new Response("Media not found",{status:404});const object=await e.BUCKET.get(row.objectKey);if(!object)return new Response("Media unavailable",{status:404});return new Response(object.body,{headers:{"content-type":row.contentType,"cache-control":"public, max-age=3600, stale-while-revalidate=86400","content-disposition":`inline; filename="${row.fileName.replace(/"/g,"")}"`,"x-content-type-options":"nosniff"}})}
