import{getChatGPTUser}from"../../../chatgpt-auth";async function env(){return(await import("cloudflare:workers")).env}async function owner(){return(await getChatGPTUser())?.email.toLowerCase()==="attriassociates99@gmail.com"}
export async function GET(){if(!await owner())return Response.json({error:"Unauthorized"},{status:401});const r=await(await env()).DB.prepare("SELECT id,email,full_name AS fullName,role,department,permissions_json AS permissionsJson,status FROM staff_members ORDER BY full_name").all();return Response.json({staff:r.results})}
export async function PATCH(request:Request){if(!await owner())return Response.json({error:"Unauthorized"},{status:401});const b=await request.json()as{id?:number;permissions?:string[]};if(!b.id||!Array.isArray(b.permissions))return Response.json({error:"Invalid permissions."},{status:400});const allowed=["dashboard","crm","projects","appointments","commerce","courses","finance","reports","content","files","analytics","settings"];const permissions=b.permissions.filter(x=>allowed.includes(x));const e=await env();await e.DB.prepare("UPDATE staff_members SET permissions_json=? WHERE id=?").bind(JSON.stringify(permissions),b.id).run();return Response.json({success:true})}
