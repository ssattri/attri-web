import {getRegisteredAccount} from "../../../../../auth";
import {activateConsultation,ensureConsultationPayments} from "../../../../../consultation-payment";
import {configured,equal,hmac,razorpayEnv,razorpayFetch} from "../../../../../razorpay";
export async function POST(request:Request){const origin=request.headers.get("origin");if(origin&&origin!==new URL(request.url).origin)return Response.json({error:"Invalid request origin."},{status:403});const user=await getRegisteredAccount();if(!user||user.accountType!=="user")return Response.json({error:"Unauthorized"},{status:401});const b=await request.json()as{razorpay_payment_id?:string;razorpay_order_id?:string;razorpay_signature?:string};if(!b.razorpay_payment_id||!b.razorpay_order_id||!b.razorpay_signature)return Response.json({error:"Incomplete payment confirmation."},{status:400});const e=await razorpayEnv();if(!configured(e))return Response.json({error:"Payment gateway unavailable."},{status:503});const db=await ensureConsultationPayments(),attempt=await db.prepare("SELECT id,amount,razorpay_order_id AS orderId FROM consultation_payment_attempts WHERE razorpay_order_id=? AND lower(customer_email)=? LIMIT 1").bind(b.razorpay_order_id,user.email.toLowerCase()).first<{id:number;amount:number;orderId:string}>();if(!attempt)return Response.json({error:"Payment order not found."},{status:404});const expected=await hmac(`${attempt.orderId}|${b.razorpay_payment_id}`,e.RAZORPAY_KEY_SECRET||"");if(!equal(expected,b.razorpay_signature))return Response.json({error:"Payment verification failed."},{status:400});const payment=await razorpayFetch(`/payments/${encodeURIComponent(b.razorpay_payment_id)}`,e);if(payment.order_id!==attempt.orderId||Number(payment.amount)!==attempt.amount||payment.currency!=="INR")return Response.json({error:"Payment details do not match."},{status:400});if(payment.status!=="captured"){await db.prepare("UPDATE consultation_payment_attempts SET razorpay_payment_id=?,status=?,signature_verified=1,updated_at=CURRENT_TIMESTAMP WHERE id=?").bind(b.razorpay_payment_id,String(payment.status||"authorized"),attempt.id).run();return Response.json({success:true,pending:true,message:"Payment authorised. Booking will confirm after capture."})}await activateConsultation(attempt.orderId,b.razorpay_payment_id,1);const appointment=await db.prepare("SELECT a.reference FROM consultation_payment_attempts p JOIN appointments a ON a.id=p.appointment_id WHERE p.id=?").bind(attempt.id).first<{reference:string}>();return Response.json({success:true,reference:appointment?.reference})}
