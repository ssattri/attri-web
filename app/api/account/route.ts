import{getChatGPTUser}from"../../chatgpt-auth";async function db(){return(await import("cloudflare:workers")).env.DB}
async function ensure(){const d=await db();await d.prepare(`CREATE TABLE IF NOT EXISTS customer_profiles (id INTEGER PRIMARY KEY AUTOINCREMENT,email TEXT NOT NULL UNIQUE,full_name TEXT NOT NULL,phone TEXT NOT NULL DEFAULT '',company TEXT NOT NULL DEFAULT '',address TEXT NOT NULL DEFAULT '',city TEXT NOT NULL DEFAULT '',state TEXT NOT NULL DEFAULT '',pincode TEXT NOT NULL DEFAULT '',gstin TEXT NOT NULL DEFAULT '',status TEXT NOT NULL DEFAULT 'active',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();const columns=await d.prepare("PRAGMA table_info(customer_profiles)").all<{name:string}>();if(!columns.results.some(x=>x.name==="account_type"))await d.prepare("ALTER TABLE customer_profiles ADD COLUMN account_type TEXT NOT NULL DEFAULT ''").run();return d}
export async function GET(){const u=await getChatGPTUser();if(!u)return Response.json({error:"Unauthorized"},{status:401});const d=await ensure();const p=await d.prepare("SELECT account_type AS accountType FROM customer_profiles WHERE lower(email)=?").bind(u.email.toLowerCase()).first<{accountType:string}>();return Response.json({accountType:p?.accountType||""})}
export async function POST(request:Request){const u=await getChatGPTUser();if(!u)return Response.json({error:"Unauthorized"},{status:401});const b=await request.json()as{role?:string};if(!["user","consultant"].includes(b.role||""))return Response.json({error:"Choose a valid account type."},{status:400});const d=await ensure();const email=u.email.toLowerCase();const existing=await d.prepare("SELECT account_type AS accountType FROM customer_profiles WHERE lower(email)=?").bind(email).first<{accountType:string}>();if(existing?.accountType&&existing.accountType!==b.role)return Response.json({error:"This email is already registered with a different account type."},{status:409});await d.prepare(`INSERT INTO customer_profiles (email,full_name,account_type,status) VALUES (?,?,?,'active') ON CONFLICT(email) DO UPDATE SET full_name=excluded.full_name,account_type=CASE WHEN customer_profiles.account_type='' THEN excluded.account_type ELSE customer_profiles.account_type END,updated_at=CURRENT_TIMESTAMP`).bind(email,u.fullName||u.displayName,b.role).run();return Response.json({success:true,accountType:b.role})}
