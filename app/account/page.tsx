import{requireChatGPTUser}from"../chatgpt-auth";import{redirect}from"next/navigation";
export const dynamic="force-dynamic";export default async function AccountRouter(){const u=await requireChatGPTUser("/account");const d=(await import("cloudflare:workers")).env.DB;await d.prepare(`CREATE TABLE IF NOT EXISTS customer_profiles (id INTEGER PRIMARY KEY AUTOINCREMENT,email TEXT NOT NULL UNIQUE,full_name TEXT NOT NULL,phone TEXT NOT NULL DEFAULT '',company TEXT NOT NULL DEFAULT '',address TEXT NOT NULL DEFAULT '',city TEXT NOT NULL DEFAULT '',state TEXT NOT NULL DEFAULT '',pincode TEXT NOT NULL DEFAULT '',gstin TEXT NOT NULL DEFAULT '',status TEXT NOT NULL DEFAULT 'active',created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)`).run();const columns=await d.prepare("PRAGMA table_info(customer_profiles)").all<{name:string}>();if(!columns.results.some(x=>x.name==="account_type"))await d.prepare("ALTER TABLE customer_profiles ADD COLUMN account_type TEXT NOT NULL DEFAULT ''").run();const p=await d.prepare("SELECT account_type AS accountType FROM customer_profiles WHERE lower(email)=?").bind(u.email.toLowerCase()).first<{accountType:string}>();if(p?.accountType==="consultant")redirect("/consultant");if(p?.accountType==="user")redirect("/client");redirect("/client/login")}
