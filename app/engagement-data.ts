import{learningDb}from"./learning-data";
export async function ensureEngagementData(){const db=await learningDb();await db.batch([
 db.prepare("CREATE TABLE IF NOT EXISTS portal_reviews(id INTEGER PRIMARY KEY AUTOINCREMENT,reference TEXT NOT NULL UNIQUE,customer_email TEXT NOT NULL,customer_name TEXT NOT NULL DEFAULT '',account_type TEXT NOT NULL DEFAULT 'user',target_type TEXT NOT NULL DEFAULT 'general',target_key TEXT NOT NULL DEFAULT '',target_name TEXT NOT NULL DEFAULT '',rating INTEGER NOT NULL DEFAULT 5,title TEXT NOT NULL DEFAULT '',review TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'pending',admin_reply TEXT NOT NULL DEFAULT '',featured INTEGER NOT NULL DEFAULT 0,created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP,updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)"),
 db.prepare("CREATE TABLE IF NOT EXISTS tax_settings(id INTEGER PRIMARY KEY CHECK(id=1),legal_name TEXT NOT NULL DEFAULT '',gstin TEXT NOT NULL DEFAULT '',pan TEXT NOT NULL DEFAULT '',registered_address TEXT NOT NULL DEFAULT '',state_code TEXT NOT NULL DEFAULT '',default_gst_rate INTEGER NOT NULL DEFAULT 18,default_service_sac TEXT NOT NULL DEFAULT '998311',invoice_prefix TEXT NOT NULL DEFAULT 'AAVC',invoice_terms TEXT NOT NULL DEFAULT '',updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)"),
 db.prepare("INSERT OR IGNORE INTO tax_settings(id) VALUES(1)")
 ]);const support=await db.prepare("PRAGMA table_info(support_tickets)").all<{name:string}>();for(const[name,sql]of[["account_type","ALTER TABLE support_tickets ADD COLUMN account_type TEXT NOT NULL DEFAULT 'user'"],["issue_area","ALTER TABLE support_tickets ADD COLUMN issue_area TEXT NOT NULL DEFAULT ''"],["page_url","ALTER TABLE support_tickets ADD COLUMN page_url TEXT NOT NULL DEFAULT ''"],["device_info","ALTER TABLE support_tickets ADD COLUMN device_info TEXT NOT NULL DEFAULT ''"],["assigned_to","ALTER TABLE support_tickets ADD COLUMN assigned_to TEXT NOT NULL DEFAULT ''"],["admin_reply","ALTER TABLE support_tickets ADD COLUMN admin_reply TEXT NOT NULL DEFAULT ''"],["updated_at","ALTER TABLE support_tickets ADD COLUMN updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP"]]as const)if(!support.results.some(x=>x.name===name))await db.prepare(sql).run();const invoice=await db.prepare("PRAGMA table_info(invoices)").all<{name:string}>();for(const[name,sql]of[["gstin","ALTER TABLE invoices ADD COLUMN gstin TEXT NOT NULL DEFAULT ''"],["hsn_sac","ALTER TABLE invoices ADD COLUMN hsn_sac TEXT NOT NULL DEFAULT ''"],["taxable_amount","ALTER TABLE invoices ADD COLUMN taxable_amount INTEGER NOT NULL DEFAULT 0"],["cgst_amount","ALTER TABLE invoices ADD COLUMN cgst_amount INTEGER NOT NULL DEFAULT 0"],["sgst_amount","ALTER TABLE invoices ADD COLUMN sgst_amount INTEGER NOT NULL DEFAULT 0"],["igst_amount","ALTER TABLE invoices ADD COLUMN igst_amount INTEGER NOT NULL DEFAULT 0"]]as const)if(!invoice.results.some(x=>x.name===name))await db.prepare(sql).run();return db}
export function validGstin(value:string){return !value||/^[0-9]{2}[A-Z]{5}[0-9]{4}[A-Z][1-9A-Z]Z[0-9A-Z]$/.test(value.toUpperCase())}
