"use client";import{useEffect,useState}from"react";export default function ConsultationSessionList(){const[list,setList]=useState<any[]>([]);useEffect(()=>{fetch("/api/consultations/session").then(r=>r.json()).then(d=>setList(d.sessions||[]))},[]);return <section className="portal-consultation-list">{list.length?list.map(x=><a href={`/consultations/${x.reference}`} key={x.reference}><div><small>{x.consultationMode} · {x.reference}</small><b>{x.consultantName||x.userName}</b><span>{x.scheduledDate} · {x.scheduledTime}</span></div><strong className={x.status}>{x.status}</strong><em>Open room →</em></a>):<p>No consultation sessions yet.</p>}</section>}
