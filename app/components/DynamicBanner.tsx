"use client";import{useEffect,useState}from"react";export default function DynamicBanner({placement="home-top"}:{placement?:string}){const[list,setList]=useState<any[]>([]);useEffect(()=>{fetch(`/api/platform?placement=${encodeURIComponent(placement)}`).then(r=>r.json()).then(d=>setList(d.banners||[]))},[placement]);if(!list.length)return null;const b=list[0];return <aside className={`dynamic-banner ${b.category}`} style={{backgroundImage:b.desktopImage?`linear-gradient(90deg,rgba(12,30,25,.9),rgba(12,30,25,.25)),url(${b.desktopImage})`:undefined}}><div><small>{b.category}</small><h2>{b.heading||b.title}</h2>{b.subheading&&<p>{b.subheading}</p>}{b.ctaLabel&&b.targetUrl&&<a href={b.targetUrl}>{b.ctaLabel} →</a>}</div></aside>}
