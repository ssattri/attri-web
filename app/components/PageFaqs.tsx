type FAQ={id:number;question:string;answer:string;category:string};async function getFaqs(pageSlug:string){try{const database=(await import("cloudflare:workers")).env.DB;return(await database.prepare("SELECT id,question,answer,category FROM faqs WHERE status='published' AND (page_slug=? OR page_slug='all') ORDER BY category,sort_order,id").bind(pageSlug).all<FAQ>()).results}catch{return[]}}export default async function PageFaqs({pageSlug}:{pageSlug:string}){const faqs=await getFaqs(pageSlug);if(!faqs.length)return null;return <section className="managed-page-faq" id="faq"><div><p>FREQUENTLY ASKED QUESTIONS</p><h2>Useful answers<br/>for your next step.</h2><span>Managed centrally by our team and organised for this page.</span></div><div>{faqs.map(faq=><details key={faq.id}><summary>{faq.question}<span>+</span></summary><p>{faq.answer}</p><small>{faq.category}</small></details>)}</div></section>}
