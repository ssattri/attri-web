"use client";import{FormEvent,useEffect,useState}from"react";
type Row=Record<string,string|number>;
export default function PortalEngagement({mode="both"}:{mode?:"both"|"support"|"reviews"}){const[data,setData]=useState<{reviews:Row[];tickets:Row[]}>({reviews:[],tickets:[]}),[message,setMessage]=useState("");async function load(){const r=await fetch("/api/portal/engagement");if(r.ok)setData(await r.json())}useEffect(()=>{void load()},[]);async function send(e:FormEvent<HTMLFormElement>,action:string){e.preventDefault();const form=e.currentTarget,r=await fetch("/api/portal/engagement",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,...Object.fromEntries(new FormData(form))})}),d=await r.json();setMessage(r.ok?`${d.message} Reference: ${d.reference}`:d.error);if(r.ok){form.reset();await load()}}
 return <div className="portal-engagement">{message&&<div className="portal-engagement-toast">{message}<button onClick={()=>setMessage("")}>×</button></div>}{mode!=="reviews"&&<section><form onSubmit={e=>send(e,"ticket")}><p>FUNCTIONALITY SUPPORT</p><h2>Report an issue</h2><div className="portal-form-grid"><label>Subject<input name="subject" required placeholder="What is not working?"/></label><label>Category<select name="category"><option>Account & login</option><option>Consultation</option><option>Order & payment</option><option>Course & LMS</option><option>Vastu software</option><option>Report & download</option><option>Technical support</option><option>Other</option></select></label><label>Issue area<input name="issueArea" placeholder="Page, feature or module"/></label><label>Priority<select name="priority"><option value="normal">Normal</option><option value="high">High</option><option value="urgent">Urgent</option></select></label><label className="wide">Page URL<input name="pageUrl" type="url" placeholder="https://attriassociates.com/..."/></label><label className="wide">Error details<textarea name="message" required rows={5} placeholder="What happened, what you expected, and any error message"/></label><label className="wide">Device / browser<input name="deviceInfo" placeholder="Example: Windows 11 · Chrome"/></label></div><button>Submit support request</button></form><RecordList title="My support requests" rows={data.tickets} kind="ticket"/></section>}{mode!=="support"&&<section><form onSubmit={e=>send(e,"review")}><p>RATING & FEEDBACK</p><h2>Share your experience</h2><div className="portal-form-grid"><label>Review for<select name="targetType"><option value="general">Overall experience</option><option value="product">Product</option><option value="course">Course</option><option value="consultation">Consultation</option><option value="software">Vastu software</option><option value="service">Architecture / Vastu service</option></select></label><label>Name of product/service<input name="targetName" placeholder="Example: Residential Vastu Consultation"/></label><label>Rating<select name="rating" defaultValue="5"><option value="5">★★★★★ Excellent</option><option value="4">★★★★ Very good</option><option value="3">★★★ Good</option><option value="2">★★ Needs improvement</option><option value="1">★ Poor</option></select></label><label>Review title<input name="title" placeholder="Summarise your experience"/></label><label className="wide">Your feedback<textarea name="review" required minLength={20} rows={5} placeholder="Tell us what was useful and what we can improve"/></label></div><button>Submit review for approval</button><small>Reviews appear publicly only after administrator approval.</small></form><RecordList title="My reviews" rows={data.reviews} kind="review"/></section>}</div>}
function RecordList({title,rows,kind}:{title:string;rows:Row[];kind:string}){return <div className="portal-engagement-list"><h3>{title}</h3>{!rows.length?<p>No records yet.</p>:rows.map(x=><article key={String(x.reference)}><div><b>{kind==="review"?`${"★".repeat(Number(x.rating))} ${x.title||x.targetName}`:x.subject}</b><small>{x.reference} · {x.category||x.targetType} · {String(x.createdAt).slice(0,10)}</small>{x.adminReply&&<p><strong>Attri response:</strong> {x.adminReply}</p>}</div><span>{x.status}</span></article>)}</div>}
